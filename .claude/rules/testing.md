# Testing Rules

- Write tests for all new logic code
- Run tests before claiming work is complete
- Test edge cases, not just happy paths
- Test failures should include clear error messages
- Enforced by: PostToolUse auto-test hook + TaskCompleted hook
